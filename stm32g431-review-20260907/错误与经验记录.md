# 错误与经验记录：STM32G431 工程

日期：2026-09-07。此文件分轮记录审核和实际修改中的错误；电路问题沿用 S01–S20，当前状态见 [修订记录](STM32G431原理图修订记录-20260907.md)。首轮只读审核的历史记录保留如下。

旧工程记录保留在 `../foc-redesign/错误与经验记录.md`。两个项目的电路缺陷不可合并套用。

| 编号 | 发生的问题 | 影响 / 当前处置 | 防复发规则 |
|---|---|---|---|
| E01 | 当前工程未确认前曾沿用旧 FOC 的 215 元件快照判断 LDO/电源 | 用户明确选择新 STM32G431 工程后撤销适用性；正式报告只使用新工程的 206 元件网表 | 每次检查先读项目 UUID、页 UUID、主控与电源型号；不能依据旧对话推断当前工程 |
| E02 | Bridge 未接通时尝试读取本地 eprj2/SQLite，但图形历史内容编码存储 | 只做备份和只读检查；未能形成可靠的当前电路证据，未改动工程数据库；恢复连接后改用 live API | 不把编码历史表当作完整网表，不用推测解码结果出具审核结论 |
| E03 | 打开 Driver 页尚未完成即启动依赖读取，导致页信息和内容可能混合 | `driver-live.json` 不用于页级结论；重读为 `driver-verified-live.json`，最后读数再次一致 | 页切换必须 await 完成；读取前后都校验页和项目；发生切换则整次读取作废 |
| E04 | 请求 PDF 导出却收到 ZIP，最初按 PDF 扩展名保存 | 检查实际文件头 PK 后改为 ZIP 并提取三页 PDF；已逐页渲染检查 | 输出类型、返回文件名和 magic bytes 三者核验，不能仅相信请求参数 |
| E05 | 试用未安装的 fitz 模块；一次 PowerShell 探查引用格式错误 | 两者均在本地只读分析阶段失败，未影响原理图；改用已提供的 pypdfium2 | 先确认现有运行库；避免复杂跨层转义，分析代码写独立脚本 |
| E06 | 一个摘要脚本直接索引可选 `mpn` 字段，遇到通用器件时报 KeyError | 改成 `.get()` 后重新输出；确认 80 个元件未给供应商料号，不能假定其规格已齐全 | 分析器处理可选属性；区分未绑定物料与电气错误 |
| E07 | 数据表链接可能失效或返回其他料号资料 | 失效的 TVS 链接未当作已读原厂证据；此次 113V 参数来自实际绑定物料属性 | 每份 PDF 核对标题、型号与修订版；区分厂商手册、库属性、计算和待验证假设 |

首轮只读审核结束检查：四份有效网表规范化散列相同；206 个有效元件；DRC 仅有警告汇总，未取得明细。该轮未执行原理图、元件、标签或导线的写入。

后续补充时保留历史行；每条增加日期、根因、修复证据与状态，不将“接口成功”或“计算通过”直接改写为电路已验证。

最新 LDO 修订及证据见 [机器人控制器 LDO 选型与修订](机器人控制器LDO选型与修订-20260907.md)，202 个元件。后文 203 元件、AZ1117C 和旧 PDF 的描述属于上一轮历史结果。

## 第二轮：实际修改，2026-09-07

| 编号 | 发生的问题 | 处置与证据 | 防复发规则 |
|---|---|---|---|
| E08 | Bridge 写入超时，或返回成功但修改未落图；曾留下旧 C29 | 用户保持当前工程前台后继续；最终 C29 绑定 C12891、旧件及中间临时件均已清除 | 每次写入必须由另一次调用回读；成功返回值只表示请求结果，不表示工程已持久化 |
| E09 | 对新建器件调用 component.modify 移动/改属性，导致料号、封装及属性关联丢失或改变；出现 CL31A226KAHNNNE.1 等错误关联 | 弃用受损实例，按最终坐标重新由实际库料号创建，逐个修改 Designator/Name/Unique ID 属性；[最终核验](revision-verification.json) 检查料号、封装和装配属性 | 不将显示名称修改当成物料替换；本 Bridge 版本优先新建绑定器件再替换，保留旧 Unique ID；新件验证前不删旧件 |
| E10 | 链式 setState_X/Y/Name.done() 返回后状态未变化；某次完整元件属性修改抛出 cmdKey 解构异常 | 停止该方式；改用单个属性图元对象修改和新建器件；异常作为失败记录 | 新接口先改一个对象并独立回读，再扩展；同一失败路径不盲目批量重试 |
| E11 | 不同库电容符号默认方向不同，相同 rotation 得到不同引脚方向；C24/C59 等试放件未匹配旧焊点 | 改为读取实际引脚坐标，校正创建角度后重新放置；临时实例已删除 | 两端无极性器件按几何端点匹配，允许引脚编号互换；不能仅比较 rotation 或猜测引脚间距；坐标先容差取整 |
| E12 | wire.modify 传 ID 常未生效；传对象修改多段线也出现未改变/创建失败 | 对简单线使用对象；对失败的多段组读取全部独立段，删除该组并逐段重建。U/V/W 相线及 POWER/Driver 残线均重新核验 | Line 扁平数组每 4 个数字是一条独立线段，不是连续折线；删除前保存所有段和网络/标签，不遗漏分支 |
| E13 | SPI 网络标签已改名，但导线实际网络或连接仍旧，造成“看起来已经改好” | 后续修改实际导线对象；验证 MCU、两编码器和跨页控制的引脚网络 | 标签显示值与线网 net 都要检查；不能用改文字替代接线 |
| E14 | 写入后立即导出的网表滞后，U 相首次回读一度显示开路，第二次独立读取才反映新线 | 没有根据首次旧快照重复创建；读取 source/model 后再导出网表，最终三页快照一致 | 遇到真假不一致先辨别缓存；先回读，后决定是否重试，避免重复元件和重复导线 |
| E15 | EDA 脚本 catch 返回 `{error}`，外层 runner 仍以进程成功退出，可能继续执行依赖步骤 | run-read.cjs 已在保存错误详情后检测 result.error 并退出失败；依赖 PowerShell 调用使用 LASTEXITCODE 门控 | 失败信息和控制流必须一致，不能把保存了错误 JSON 当成执行成功 |
| E16 | BEAD3 以 ID 删除返回 true，但独立 PDF/网表仍有该器件，且留下断开的端点 | 使用实际器件对象删除，203 元件版本已无 BEAD3；线头也改用删除导线组后逐段重建清理 | 删除也需独立验证对象消失、连接完整和导出持久化，不能只验证 create/modify |
| E17 | 曾把 source 中 Footprint:null 等继承属性理解为绑定丢失 | 正常旧件也有继承 null；以 live footprint、Supplier Part、网表与工程导出共同判定 | 区分默认继承、空值、实际断绑；不因单一 source 字段为 null 重建正常元件 |
| E18 | 本轮初稿 C76 电源旗标越过图框；C44、C39/C40、R2 与电流滤波文字出现拥挤；删 RC 留下悬空线头 | 导出 PDF 发现后，将 C76 收回框内、错开放置标签/位号，隐藏六个重复 ESD 名称并加统一说明，删除实际残线 | 单个功能块完成后必须看图；标签位置需要预留宽度，不得仅按引脚坐标放文字。先验证字号单位，不因拥挤随意缩字 |
| E19 | 多个布局修正返回成功后，部分多段残线仍然存在 | 用 source 中的实际线段复查，而非复用预期计划；最终删除/重建并再次导出 | 修改方案文件不等于实际图；复核必须来自新的 source/网表/PDF |
| E20 | DRC 请求详细信息仍只给汇总，修订后为 warn:2，尚不能定位警告对象 | 如实保留，未通过清除标记或降低规则掩盖；[final-drc.json](final-drc.json) | 自定义连接检查通过不等于 ERC/DRC 通过；未知警告保持未闭合 |
| E21 | 工程导出与 live 原始文本逐字不同，初看可能误认为未保存 | 差异来自 zIndex、默认 strikeout、字号字符串化和浮点取整；语义字段一致。导出包含不属于 live 线组的内部 LINE 残留，单独记录而非当作有效画布连线 | 按已观察的格式差异做有限规范化；不能盲目忽略实际网络、器件绑定或坐标差异 |

| E22 | 末次 PDF 发现删除吸收支路后仍有两个独立悬空 PGND 符号，标题 modify 也没有落图 | 用对象删除两个旗标，标题改为创建新文本、删除旧文本；当前 source 和 PDF 已正确。核验增加对应断言 | 清理范围包含导线端的旗标/标签；标题内容也要做 source 断言，不能只验网表 |
| E23 | 旧标题在当前 live source/get() 中消失，但 epro2、PDF 导出仍存在；关闭重开图页后旧标题回来了。重复 ID/对象/数组删除和保存均未持久化 | 保留 [故障原始工程](STM32G431-caption-bug-raw.epro2) 及 [故障 PDF](STM32G431-caption-bug-raw.pdf)。期间制作过仅清理旧文本的临时导出副本，但没有用它作为最终交付。最终通过官方 setDocumentSource 仅删除当前 POWER 页指定旧 TEXT，关闭重开确认消失，网表仍相同；重新导出的 PDF/epro2 已正确，最终文件未离线改写 | 仅做 live/get 回读仍不够；导出和关闭重开是独立的持久化检查。发现差异必须修复应用状态，不能只清理副本掩盖。源码接口操作前备份并限定唯一记录，操作后核验整页连接与导出 |
| E24 | 个别只读探查使用了旧/不存在的脚本名、错误的参考文档子目录，或对可选 ref 属性直接索引，导致读取失败 | 改用 rg 定位实际文件及可选字段读取后继续；这些失败没有触发原理图写入 | 先确认文件路径与返回结构；读取错误不改变电气结论，不让探查失败触发依赖写入 |
| E25 | 新母线开关标题比原文长，创建时使用的文字对齐使其靠近分隔线并碰到两侧 PGND 标签 | 根据导出图面将标题收进两地符号间的留白，调整为 14 号、x=850/y=375，使用已验证的官方源码接口更新后关闭重开确认 | 不能只照搬旧文本的中心坐标和字号；新文字长度及真实对齐规则也决定占用范围 |

本轮收尾：三页完整网表一致，203 个有效元件，74 项针对性检查通过；无计划外连接变化，PCB 导出与修改前一致。14S/58.8V 已获用户确认并写入图纸。最终图纸与完整工程均重新从修复后的应用导出，未离线改写。固件/PCB 对接及 S01–S20 剩余问题见 [修订记录](STM32G431原理图修订记录-20260907.md)。电气保护、热设计和台架验证的开放项仍未完成。

## 第三轮：机器人 LDO 对标与修订，2026-09-07

| 编号 | 发生的问题 | 处置与证据 | 防复发规则 |
|---|---|---|---|
| E26 | 前轮将 AMS1117 改为 AZ1117C 时重点考虑陶瓷输出电容兼容性，未充分纳入该型号 −20°C 工作结温下限 | 本轮查阅真实机器人/伺服控制器公开图纸，综合温度、热阻与电容选择 LDL1117S33R，实际 C435835 已保存；宽温选择不等同于整板低温验收 | 选 LDO 同时记录输入、负载、实际损耗、热环境、有效容量/ESR、工作结温与精确后缀；不要只检查一项后宣布选型完善 |
| E27 | 新 LDL1117 库符号的 1/2/3 脚全在左侧，与旧符号几何位置不同；仅改 Symbol 属性未立即改变实际引脚坐标 | 保留旧件，先读新件全部引脚；用官方源码接口同步指定实例的 partId 与已验证同引脚功能的符号覆盖，保持真实 ST Device/封装；重开后核验四脚和 BOM，才删除旧件 | 同封装不等于同符号坐标；若复用功能符号，必须证明引脚编号/功能一致，并分别核验显示符号、真实物料和封装，不能只改名称 |
| E28 | 先写回原 Unique ID，再启用 BOM/转 PCB 后，系统又生成 gge368，覆盖了预期 gge191 | 在装配属性启用完成后最后写回 gge191；独立网表和官方 epro2 已确认，最终没有临时 ID 或重复位号 | 身份恢复放在可能自动编号的步骤之后；必须从持久化网表/导出文件验证，不能依赖 setter 返回值 |
| E29 | 输入电容 C38 的旧标签与 C43 本体/导线拥挤；R82 位号贴到电阻；属性 API 的 align 修改返回成功但源码仍是 null | 官方画布截图发现后，将 C38 标签移到左侧，R82 两行文字放在本体下方；用官方源码接口限定更新 8 个标签坐标/对齐/可见性，工程导出已核实 | 元件紧凑时应左右分配标签；新属性对齐需核对枚举与落盘值，并通过实际图面检查；不能只按中心坐标判断无碰撞 |
| E30 | Bridge 中途退出；MCU 打开、网表和 PDF 导出多次超时；画布抓图先返回旧帧，最后返回全透明 PNG；一次重载期间连接 ID 变更 | 用户重连并手动打开 MCU 后，独立网表和完整工程导出恢复，25 项检查通过。最终重载/连接恢复后再次核实工程。PDF 和最后标签视觉复核仍标为未完成，透明 PNG 不作为交付图 | 对长操作以文件内容/页 UUID/非空图像为准；每次重连重新枚举窗口。透明图与缓存帧不能证明当前画布。无法获得新 PDF 时明确区分旧版文件和本轮工程 |
| E31 | 只读研究脚本出现未匹配括号、CSV 编码错误，以及 Windows 下把文件通配符直接交给 rg；部分原厂 PDF 下载返回 567/403 | 修正后读取官方版本 BOM，使用 cp1252；rg 改为目录加 -g；失败链接不作为已下载证据。技术参数来自成功打开的原厂手册，保留下载失败记录 | 分开研究与写入，研究脚本失败不得触发器件修改；先检查编码、返回格式与文件存在性，避免打印整份超大 JSON |

本轮电气/工程证据：[robot-ldo-verification.json](robot-ldo-verification.json)。已保存 U9=C435835、202 个有效元件；未修改 PCB/固件，也未完成热稳态、阶跃响应或整板保护验证。

## 第四轮：CAN 数字端接与本地 API 工具层，2026-09-07

| 编号 | 发生的问题 | 处置与证据 | 防复发规则 |
|---|---|---|---|
| E32 | 首次接线时 PB10 还保留 NC，调用在该引脚附近中止；清除 NC 后，立即回读又未见新线，而随后独立读取已存在 | 清除并独立核验 NC，按现存线段继续；不能把第一次中止唯一归因于 NC，已确认的另一原因是回读缓存滞后。中断和恢复均留在 eda-verified/state 日志 | 先消除 NC；写一次，独立读取；不把瞬时 get() 未命中直接当作未落图，不盲目重建 |
| E33 | 新工具最初只报告 HTTP 500，丢失服务端具体原因；一个测试把错误文本 window 写成 windows，导致断言误报 | 工具保留 HTTP 响应 JSON；修正断言。最终 11 项工具测试通过，包括 HTTP 错误、只重试读取和过期计划阻止写入 | 保留原始错误并返回非零状态；检查测试失败原因，不把日志文件存在当作操作成功 |
| E34 | R6 新建后虽尝试写回 gge166，首次保存仍自动分配成 gge374 | 独立网表抓到不一致；在初始化/装配落盘后单独恢复 gge166，再经官方工程导出验证。见 can-termination-verification.json | 原件身份恢复放在可能触发自动编号的步骤之后，保留唯一性与持久化断言 |
| E35 | 布局初稿按截图估计下边界 215，实际编码器图框边界为 190，光继电器上方标签侵入邻区 | 从实际 RECT 记录核对边界，将整个新模块下移 30，增加独立图框；元件、引线、标签和注释一起移动，网表不变 | 不从截图比例猜坐标；先读取区域边界再放置，整个功能块一起变换并核验引脚连通 |
| E36 | 整体平移源码两次被 setDocumentSource 返回 false；根因是修改最后一条 TEXT 时额外添加了原文件没有的末尾竖线 | 离线只读对比定位末记录格式，保留每条原分隔符后 API 写入成功；源码工具增加 CRLF/末记录/删除记录保留测试 | JSON 能解析不等于 EPRU 容器格式有效；保留原有记录终止方式，不统一补竖线 |
| E37 | NET 标签 valueVisible=false 经保存/导入又恢复显示；早期只做电气核验，没抓到全部重复文字 | 删除多余 NET 属性；模块内部四个短线节点改用匿名网络，保留六个跨块标签；新增可见标签数量与内部节点互异检查 | 图面要求也必须进入核验断言；需要删除冗余对象时不能仅改隐藏状态或移走文字 |
| E38 | PDF/SVG 导出各超时；第一次截图是修改前缓存画面，重开后截图全透明 | 电气和完整工程导出正常，图面截图单独标记未验收，已请求前台再核对；未将旧图或透明图作为最终图 | 抓图同时检查内容新鲜度和透明度；文档/PDF/GUI/网表的证据状态分开，不能以其中之一替代其他 |
| E39 | 完整 epro2 导出含空体 LINE 删除记录，首版校验器直接 JSON 解包失败；若干只读库搜索返回近似型号、PowerShell 大对象输出截断、rg 没有匹配 | 校验器保留删除记录并比较，实际物料按精确 C 编码重新核实；输出改为限定字段。这些读取故障未作为接线依据 | 保留导出格式中的删除标记；查询结果必须核对精确料号与封装；不要打印整份大型网表 |

当前完成证据：[CAN 数字端接核验](can-termination-verification.json)，43 项通过、208 元件、原样工程导出。最终实际画面、PCB 同步、固件实现和台架测试仍按各自状态追踪。

## 第五轮：双编码器只读审查，2026-09-07

| 编号 | 发生的问题 | 处置与证据 | 防复发规则 |
|---|---|---|---|
| E40 | 研究读取遇到旧 ams 官方 PDF 地址 404；资料合并输出截断；一次 functions.exec 编排括号错误在执行前被拒绝 | 改读原厂 PDF 的 Mouser 托管版，检查正文版本；修正括号后读取成功，最终单独输出 19 项检查摘要。失败编排没有执行任何原理图操作 | 核对实际资料正文版本；控制输出字段，编排语法错误与电路问题分开，不把搜索摘要当成已打开手册 |
| E41 | 前轮主要修复 SPI 引脚映射和片选上拉，没有闭合 SCK/MOSI 的启动电平及本模块未绑定 BOM | 本次沿实际网表确认公共 SCK/MOSI 没有外部接地偏置，12 个外围阻容未绑定料号；已列入双编码器审查的待完善项，尚未修改 | 接线修复完成不代表整个模块设计完结；同时追踪启动/复位状态、生产物料、机械安装与固件诊断 |

## 第六轮：双编码器实际修订，2026-09-08

| 编号 | 发生的问题 | 处置与证据 | 防复发规则 |
|---|---|---|---|
| E42 | 研究时凭记忆查询的 C57112 实为 10nF，不是目标 1µF；C15849 也不是预想的耐压/介质组合 | 放置前核对真实库参数，最终改用 C59782、1µF/16V/X7R；错误候选没有放入原理图 | 供应商 C 编码不能凭记忆直接写图；精确 MPN、容量、耐压、介质和封装共同匹配 |
| E43 | 第一批替换的 C16/C18 独立几何核验失败。初步表述为“方向不同”，实际是新符号引脚间距 30、旧符号 40 | 回读真实端点后补齐连接，再整理到最终去耦位置，连接核验通过。旧件在新端点明确前保留 | 区分角度变化与引脚跨度差异；不要把未定位的原因当成确定根因 |
| E44 | 阶段核验失败没有生成成功输出文件，随后尝试读该文件失败；多次原始资料输出超长截断；一条 PowerShell 的后续读取令整体退出码掩盖了前面 Python 校验失败 | 根据工具日志与实时 model 继续，后续依赖以 LASTEXITCODE 门控；失败导出对比保留为 encoder-export-differences.json | 每步成功应由结果证明；不要打印整页 JSON，不将后续只读命令的成功当作前一步成功 |
| E45 | U6 整组移动时漏处理以“组件 ID-引脚 ID”为父对象的 NC 属性；live 自动显示在新位置，完整 epro2 仍存旧坐标，相差 5 单位 | 导出比较抓到 6 项差异，未忽略。用引脚 API 分别清除、重新标记 6 个未用输出，再重新核对导出 | 移动包含引脚子属性，并检查持久化。图面与 live 自动归位不能替代文件核验 |
| E46 | 编码器截图接口先返回没有新增下拉/角色文字的旧帧，重开后返回全透明 PNG；PDF 导出再次 30 秒超时 | 不交付这些图片为新版截图；连接和完整工程核验独立进行，视觉验收明确保留 | 检查可见内容新鲜度与 alpha；不能因截图文件存在或接口返回成功就称图面通过 |

本轮补充：NC 恢复后，一次冗余回读及最终 DRC 请求也超时。采用该次恢复操作已通过的独立 verification.json 作为同一修改后的电气/源码证据，再重新从官方接口导出完整工程；45 项最终核验全部通过，6 个 NC 坐标差异已经消除。DRC 本轮未获得结果，不能把上一版 warn:2 当成本轮通过。

## 第七轮：DRC 诊断中断与客户端升级，2026-09-08

| 编号 | 发生的问题 | 处置与证据 | 防复发规则 |
|---|---|---|---|
| E47 | 将 API 文档中的“4.2 起新增”误当成可公开下载的客户端版本，先前升级建议证据不足 | 已向用户纠正。实时官网下载页和官方已签名安装包为 3.2.186；本轮从 3.2.149 升级至 3.2.186.b52e3e87，运行 API 返回 3.2.186。没有安装或声称安装 4.2 | 区分文档目标版本、公开发行版、实际安装版；没有官方安装包和发布证据，不能许诺获得某版本或其接口 |
| E48 | DRC 明细接口在 3.2.149 只返回汇总；曾将超时与缺少明细混为一种异常 | 前轮实测正常调用约 1 秒、汇总 warn:2；界面定位了供应商属性不一致及非法位号信息。3.2.186 复测约 452ms、汇总 warn:3，仍非明细。见 drc-diagnostics/drc-1788806161379.json | 分别记录通信失败、接口版本/返回结构、实际设计规则问题；新版本升级不能替代 DRC 清理，也不能将汇总数量当成已定位的器件数 |
| E49 | 原有 22 个电阻的采购料号对应 0805，但原理图 Footprint 为 R0603；项目导入后的 UUID 变化容易干扰比较 | 实际库核对后确认。已修复 POWER 页 R73–R76 为真实 R0805；Driver 页 18 个尚未执行。为了导入封装而创建的 R_DRC_IMPORT 仍在 MCU 页，BOM/PCB 均禁用，待完成后删除 | 同时核验真实采购型号和封装名称/内容；不能仅比较库 UUID。标准化显示名称不能掩盖实际封装不符 |
| E50 | 创建独立注释时把源码 align 字符串直接传给要求数字枚举的 Text API，返回 undefined；首版核验还误读 text 字段 | 改用 LEFT_MIDDLE=2、CENTER_MIDDLE=5，并从源码 TEXT.value 核验。MCU 的 DNP 与 ±1% / 0.25W 两条注释已独立验证 | API 参数枚举与源码存储枚举分别查证；返回 undefined 立即按失败处理；先单对象实测再批量 |
| E51 | POWER 页 5 条注释使用正确枚举仍返回 undefined，未落图；该轮用户按 Escape 中止电脑操作 | 已停止，不归因于已修正的枚举。C29/C24/C67 耐压注释与 J1/J2 端子角色注释待补；电容采购耐压未改变。当前升级保留这份真实中间状态，未离线补图伪造完成 | 用户中止后保留可追溯状态；下一轮先实时检查，不能重跑已成功的源码修改或盲目重复创建 |
| E52 | 官网搜索缓存仍显示 3.2.149；第一次将下载页 data-url 直接拼到主站产生 404；浏览器打开页面超时 | 直接读取实时官网下载 HTML，并根据已安装官方更新器确定 image.lceda.cn 下载地址；完整下载后校验嘉立创数字签名、产品版本和 SHA-256。错误地址未用于安装 | 实时页面、下载域名、文件版本、签名交叉核验；相对 data-url 不一定以页面域名解析；不从第三方包补猜版本 |
| E53 | 读取大型单行网表/内嵌基线脚本输出截断；PowerShell 对约 259MB 安装包全文正则扫描较慢 | 改用 Node Buffer.indexOf 确认 Inno Setup；后续 JSON 解析后只输出摘要。未修改工程 | 大文件先结构化选择字段或按字节搜索，不用“前几行”读取单行巨型 JSON |
| E54 | 升级后 openProject 返回 true 时画布仍短暂处于首页，随后 Bridge windowId 改变；旧窗口请求被拒绝 | 重新发现连接并读取当前工程与 MCU 页 UUID；完成独立网表核验和升级前后完整工程比较 | 打开返回值不代表画布已准备好；重启/切工程后重新发现窗口，按实际项目/页校验，不沿用旧 windowId |

升级验收：官方安装器退出码 0，无须重启 Windows；客户端与安装包签名有效；Bridge 已自动连接；当前 STM32G431 工程已重新打开；原有 210 个元件引脚及采购绑定核验通过；完整导出 17,325 条源码记录升级前后完全一致。详见 [升级与核验记录](嘉立创升级与核验-20260908.md)。DRC 仍有 3 条汇总警告；前轮未完成的 Driver 封装、端子位号、注释与临时器件清理保持待办，未宣布通过。

## 第八轮：官方 Skill 全面更新，2026-09-08

| 编号 | 发生的问题 | 处置与证据 | 防复发规则 |
|---|---|---|---|
| E55 | 从快捷索引找到 getExtensionFile 后，误把它放在 sys_FileManager 下调用，返回“is not a function”；此前检索的 FileManager 类文档没有该方法，却仍执行了探查 | 回到完整类文档确认它属于 SYS_FileSystem；改用 eda.sys_FileSystem.getExtensionFile 读取扩展清单，确认版本 1.0.5。没有原理图写入 | 索引中方法名不证明所属模块，必须检查完整类名和签名；运行时先验证方法存在，不能凭相似模块名拼接 |
| E56 | 重启 Bridge 后扩展没有自动接回；第一次读取 Windows 状态提示嘉立创窗口最小化 | 通过电脑操作技能恢复实际窗口，读取新状态，点击 API Gateway 的“重新连接”；界面与新版 /health 均确认已连接。未强制结束编辑器或要求用户反复操作 | 自动重连未到位时保留真实未连接状态；检查窗口可用性，恢复后重新观察再操作，不复用失效坐标；观察到最小化不等于已经证明全部历史超时根因 |

本轮完成：官方 Skill/Bridge 1.1.28、ws 8.21.3，401 个官方文件齐全，Codex Skill 已注册；399 个文件与固定官方提交逐字相同，两个文件仅有已记录的本地整合修改。11 项工具测试和 24 项实际保存/网表核验通过；升级前后完整压缩包各条目解压字节一致，17,325 条源码记录无差异。DRC 复测仍为 warn:3。Gateway 的 GitHub 最新正式发布为 1.0.5，主分支 1.0.6 未当作发布版本。详见 [官方工具全面更新记录](../skill-upgrade-20260908/官方工具全面更新记录.md)。

## 第九轮：API 增强调研中的静态发现，2026-09-08

本轮审查发现的程序缺陷与风险列在下方；未在实际工程主动复现故障，不能当作已经证实的历史原理图错误根因。此轮未部署修复、未修改电路。

| 编号 | 发现的问题 | 处置与证据 | 防复发规则 |
|---|---|---|---|
| E57 | 现用 Bridge 的窗口选择成功响应引用未定义的 activeWindowId，实际状态变量为 activeEdaWindowId；赋值与返回之间可能产生异常 | bridge-server.mjs 第 204 行静态确认；官方仓库 Issue #16 的社区补丁也包含相应修复。本轮记录为 P0 待修 | 覆盖“选择有效窗口→正常 JSON 响应→实际选择一致”的回归检查；错误分支不得掩盖已发生的状态改变 |
| E58 | 固定 30 秒超时删除请求后会丢弃迟到结果；本地核验层 finally 释放锁，两者结合可能让后续任务误判空闲 | bridge-server.mjs 第 397、449 行及 eda-verified/lib.mjs 第 94–97 行静态确认；没有本轮活跃工程并发事故的证据。列为 P0 待修 | 超时不是取消，结果未知必须持久化；保留任务 ID 和迟到结果，断线不自动重发，确认状态后再继续冲突写入 |

本轮还发现结果处理缺少窗口归属比对，应随任务机制一起补充。公开标签/枚举问题是社区环境报告，先纳入兼容测试候选，不直接断言本客户端均受影响。调研证据、拟定工具、验收和边界见 [API 增强路线图](../api-roadmap-20260908/API增强路线图.md)。端口只读扫描未取得可用 Bridge 响应，所以没有进行新的客户端能力实测；调研正常完成，不要求用户为此重复重连。

后续社区扩展调研已把现有错误映射为回归要求，见 [40 项清单](../api-roadmap-20260908/community-reference/社区MCP借鉴与完整功能清单.md)：E45 的 NC 子属性对应 B02/B06；E46 的旧帧/透明图对应 C06/C07；E49 的实际封装不符对应 B01/E02/F02；E50/E55 的参数/命名空间对应 A03；E57/E58 的窗口与超时对应 A01/A04/G02。此补充是测试设计，未执行新回归，也未把社区风险冒充为本项目已经发生的事故。
